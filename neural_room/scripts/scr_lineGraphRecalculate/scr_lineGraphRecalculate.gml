/// @description scr_lineGraphRecalculate(id);
/// @param id
function scr_lineGraphRecalculate(argument0) {

	with (argument0)
		{
		surfWidth = width - (padding * 2);
		surfHeight = height - (padding * 2);
		}


}
